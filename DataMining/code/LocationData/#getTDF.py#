import nltk
from ujson import loads, dumps
from datetime import datetime
import os

# ALGO:
# read all files in the location dir
# -read line by line
# --get location delhi/'new york'
# --line->json->tagged_text->
# ---all nouns in the tagged text-> add to the tdf dictionary for the- respective location

def processCoords():
    dir = '/Users/gaurav/Documents/Work/Projects/DataMining/uncompressed/location/'
    flist = os.listdir(dir)
    for fname in flist:
        if '.data' in fname:
            newf_name = '/Users/gaurav/Documents/Work/Projects/DataMining/uncompressed/'+fname+'.data'
            if not os.path.isfile(newf_name):
                f= process_file(dir+fname)
                coordsf = open(newf_name, 'w')
                getAllCoords(f, coordsf)


